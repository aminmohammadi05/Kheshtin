/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PagePipe } from './page.pipe';

describe('Pipe: Pagee', () => {
  it('create an instance', () => {
    let pipe = new PagePipe();
    expect(pipe).toBeTruthy();
  });
});
