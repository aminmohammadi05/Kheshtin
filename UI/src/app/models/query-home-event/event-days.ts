export interface EventDays {
    offsetLeft : number,
              offsetWidth : number,
              date: string,
              left: number,
              width: number,
              backgroundColor : string,
              opacity : number
}